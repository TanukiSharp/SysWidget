<Window x:Class="SysWidget.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:SysWidget.Services"
        Title="About SysWidget"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        FontFamily="Segoe UI"
        FontSize="13">

    <StackPanel Margin="20,16" MinWidth="280">
        <TextBlock Text="SysWidget" FontSize="20" FontWeight="SemiBold" />
        <TextBlock Text="A small always-on-top system monitor." Opacity="0.7" Margin="0,2,0,0" />

        <Grid Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Version" Opacity="0.55" Margin="0,0,16,4" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Static services:AppVersion.SemVer}"
                       FontWeight="SemiBold" Margin="0,0,0,4" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Commit" Opacity="0.55" Margin="0,0,16,0" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Static services:AppVersion.Commit}"
                       FontFamily="Consolas" FontWeight="SemiBold" />
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="CopyButton" Content="Copy" Width="88" Height="26" Margin="0,0,8,0"
                    Click="OnCopyClick" />
            <Button Content="Close" Width="88" Height="26" IsDefault="True" IsCancel="True"
                    Click="OnCloseClick" />
        </StackPanel>
    </StackPanel>
</Window>
